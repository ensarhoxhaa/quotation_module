import 'package:flutter/material.dart';

class QuotationAddDetailsScreen extends StatelessWidget {
  const QuotationAddDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
